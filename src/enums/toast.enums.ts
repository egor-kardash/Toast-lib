export enum ToastType {
  Info = 'Info',
  Warning = 'Warning',
  Error = 'Error',
  Success = 'Success',
}

export enum ToastColor {
  Info = '#9b3ed4',
  Warning = '#f4e146',
  Error = '#e35734',
  Success = '#34e39b',
}

export enum ButtonType {
  Radio = 'radio',
  InlineRadio = 'inline-radio',
  Check = 'check',
  Select = 'select',
}